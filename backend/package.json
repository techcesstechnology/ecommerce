{
  "name": "freshroute-backend",
  "version": "1.0.0",
  "description": "Backend for FreshRoute e-commerce platform",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:connection": "node test-connection.js",
    "example:crud": "node examples/crud-operations.js",
    "migrate": "sequelize-cli db:migrate",
    "migrate:undo": "sequelize-cli db:migrate:undo",
    "seed": "sequelize-cli db:seed:all"
  },
  "keywords": ["ecommerce", "freshroute", "backend"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sequelize": "^6.35.2",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
